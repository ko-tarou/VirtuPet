import React from 'react';
import ReactDOM from 'react-dom/client';  // React 18対応
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));  // createRootを使用
root.render(<App />);
